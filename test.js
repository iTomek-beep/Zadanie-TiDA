import { createImage } from "/behind.js";
import { readImage } from "/behind.js";
import { updateImage } from "/behind.js";
import { deleteImage } from "/behind.js";

const idtest = document.getElementById('img');
const srctest = document.getElementById('srcin');
const alttest = document.getElementById('altin');

/*

QUnit.test( "test()", function( assert ) {
readImage("id");
assert.equal(idFigcaption.textContent.split(" ")[1], "BlockTitleTest", "Tytuł bloku zgadza się" );
assert.equal(test(12), 1, "zgadza się");
});

*/

/*

QUnit.test('readImage()', assert => {
    assert.equal(readImage(2), 2, 'readImage()');
    console.log(document.getElementById("idin"));

});

*/